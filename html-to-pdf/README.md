Using PdfCrowd to generate PDFs
===============================

You should create a file named `config.json` like below:

```json
{
    "username": "your username",
    "apiKey": "your api key"
}
```

Find more on http://pdfcrowd.com/
